@{
    Layout = null;
}
@model IEnumerable<Entity.Colors>

<select class="form-control input-sm" id="department">
    @foreach (var item in Model)
    {
        <option value="@item.Id" @(ViewBag.Id == item.Id ? "selected" : "")>@item.Color</option>
    }
</select>


