@{
    Layout = null;
}
@model IEnumerable<Entity.VacationTypes>

<select class="form-control input-sm" id="vacationType">
    @foreach (var item in Model)
    {
        <option value="@item.Id" @(ViewBag.Id == item.Id ? "selected" : "")>@item.Name</option>
    }
</select>


